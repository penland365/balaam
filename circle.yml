general:
  build_dir: api

machine:
  environment:
    GOOGLE_API_KEY:
    DARKSKY_API_KEY:
    GITHUB_ACCESS_TOKEN:
    DATABASE_HOSTNAME:
    DATABASE_PORT: 5432
    DATABASE: balaam
    DATABASE_USERNAME: 
    DATABASE_PASSWORD:
    SlACK_CLIENT_ID:
    SLACK_CLIENT_SECRET:
    SLACK_REDIRECT_URI:
    GITHUB_CLIENT_ID:
    GITHUB_CLIENT_SECRET:
